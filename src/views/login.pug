extends base
block content 
   h1 login
        form
            input.id(type="text" required name="username" placeholder="username")
            br
            input.pw(type="password" name="password" placeholder="password")
            input(type="submit")     
