extends base
block content 
    h1 join
        form(method="POST")
            input(type="text" required name="username" placeholder="username")
            input(type="text" required name="id" placeholder="id")
            input(type="password" required name="password" placeholder="password")
            input(type="password" required name="confirm" placeholder="confirm")
            input(type="submit")
          
    hr
    div     
        span 이미 계정이 있으신가요?
        a(href="/login") Login now